







/*

// Selecionar o botão de alternância de modo escuro
const toggleDarkModeButton = document.getElementById('toggle-dark-mode');

// Selecionar a section que será afetada
const section = document.getElementById('home', 'conteudo', 'perguntas');

// Função para alternar entre os modos
toggleDarkModeButton.addEventListener('click', function() {
  // Alternar a classe 'dark-mode' na section
  section.classList.toggle('dark-mode');
  
  // Alterar o texto do botão com base no modo atual
  if (section.classList.contains('dark-mode')) {
    toggleDarkModeButton.textContent = 'Modo Claro';
  } else {
    toggleDarkModeButton.textContent = 'Modo Escuro';
  }
});






// Selecionar o botão de alternância de modo escuro
const toggleDarkModeButton = document.getElementById('toggle-dark-mode');

// Função para alternar entre os modos
toggleDarkModeButton.addEventListener('click', function() {
  // Alternar a classe 'dark-mode' no body
  document.body.classList.toggle('dark-mode');
  
  // Alterar o texto do botão com base no modo atual
  if (document.body.classList.contains('dark-mode')) {
    toggleDarkModeButton.textContent = 'Modo Claro';
  } else {
    toggleDarkModeButton.textContent = 'Modo Escuro';
  }
}); */
